doc: 'Manage Go dependencies'

properties:
  LIBRARIES:
  - 'github.com/mitchellh/gox'

targets:

  libs:
    doc: 'Install libraries'
    steps:
    - for: 'library'
      in:  'LIBRARIES'
      do:
      - print: 'Getting library #{library}'
      - $: 'go get #{library}'

