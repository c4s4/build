# NeON Build File (http://github.com/c4s4/neon)

doc: Manage build directory

properties:
  NAME:      '={filename(_BASE)}'
  BUILD_DIR: 'build'

targets:

  clean:
    doc: Clean build directory
    steps:
    - delete: =BUILD_DIR
